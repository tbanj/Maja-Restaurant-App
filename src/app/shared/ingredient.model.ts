export class Ingredient {
    // Below are property of an  ingredient class
    // public name: string ;
    // public amount: number;

    // property of a class can be initialized inside a constructor
    constructor (public name: string, public amount: number) {
        this.name = name;
        this.amount = amount;
    }

}
